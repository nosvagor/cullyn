package pages

import "nosvagor/cullyn.dev/views/pages/home"

templ Home(data home.Content) {
	<div id="home-wrapper">
		@home.Inputs(data.Inputs)
		@home.HeroText(data.HeroText)
		@home.Outputs(data.Outputs)
	</div>
}
